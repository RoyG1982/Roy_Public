Add to domain monitoring app:
- The user should be able to determine a schedule for the app to run its tests. ie every 1, 2, 3 hours, every day at 6PM etc. 
Use flask app scheduler
- Optional: the user should have the ability to register and login through their google account. 

Create docker container (image) and upload to dockerhub repo. Then try to install on new instance. remember firewall. see lesson 15.